# https://aiohttp.readthedocs.io/en/stable/
aiohttp >= 3.6.2

# The dashcam module will be coming later.
# http://zulko.github.io/moviepy/
# moviepy >= 1.0.1

# Build, Dist tools
versioneer >= 0.18
flit >= 2.2.0
